import org.junit.jupiter.api.Test;

import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.*;

class SortingAlgorithmTest {
/*
    @Test
    public void testMergeSort() {
        SortingAlgorithm<Integer> algorithmInteger = new SortingAlgorithm<>(new Integer[] {-2, 4, 199, 0, -5, 20});
        algorithmInteger.setAlgorithm("MergeSort");
       // algorithmInteger.sort();

        SortingAlgorithm<Character> algorithmInteger2 = new SortingAlgorithm<>(new Character[]{'z','a','Z','A','b'});
        algorithmInteger2.setAlgorithm("MergeSort");
      //  algorithmInteger2.sort();

        assertEquals("[-5, -2, 0, 4, 20, 199]", Arrays.toString(algorithmInteger.getArray()));
        assertEquals("[A, Z, a, b, z]", Arrays.toString(algorithmInteger2.getArray()));
    }

    @Test
    public void testQuickSort() {
        SortingAlgorithm<Integer> algorithmInteger = new SortingAlgorithm<>(new Integer[] {-2, 4, 199, 0, -5, 20});
        algorithmInteger.setAlgorithm("QuickSort");
       // algorithmInteger.sort();

        SortingAlgorithm<Character> algorithmInteger2 = new SortingAlgorithm<>(new Character[]{'z','a','Z','A','b'});
        algorithmInteger2.setAlgorithm("Quicksort");
       // algorithmInteger2.sort();

        assertEquals("[-5, -2, 0, 4, 20, 199]", Arrays.toString(algorithmInteger.getArray()));
        assertEquals("[A, Z, a, b, z]", Arrays.toString(algorithmInteger2.getArray()));
    }

    @Test
    public void bubleSort() {
        SortingAlgorithm<Integer> algorithmInteger = new SortingAlgorithm<>(new Integer[] {-2, 4, 199, 0, -5, 20});
        algorithmInteger.setAlgorithm("BubbleSort");
       // algorithmInteger.sort();

        SortingAlgorithm<Character> algorithmInteger2 = new SortingAlgorithm<>(new Character[]{'z','a','Z','A','b'});
        algorithmInteger2.setAlgorithm("BubbleSort");
      //  algorithmInteger2.sort();

        assertEquals("[-5, -2, 0, 4, 20, 199]", Arrays.toString(algorithmInteger.getArray()));
        assertEquals("[A, Z, a, b, z]", Arrays.toString(algorithmInteger2.getArray()));
    }*/
}